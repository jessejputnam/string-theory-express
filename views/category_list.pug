extends layout 

block content 
  h1= title 

  ul 
    each category in category_list 
      li 
        a(href=category._id class="check")= category.name

    else 
      li There are no categories

  a(href="/catalog/category/create") Create Category